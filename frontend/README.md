# Meeting Assistant frontend

Next.js frontend for the conversational Google Calendar assistant.

Install dependencies and run the development server with npm run dev.

Set NEXT_PUBLIC_DESCOPE_PROJECT_ID in .env.local and configure the backend URL used by src/lib/api.ts.

Open http://localhost:3000, sign in, and connect Google Calendar.
